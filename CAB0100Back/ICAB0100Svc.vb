Imports System.ServiceModel

' NOTE: You can use the "Rename" command on the context menu to change the interface name "ICAB0100Svc" in both code and config file together.
<ServiceContract()>
Public Interface ICAB0100Svc

    <OperationContract()>
    Sub DoWork()

End Interface
